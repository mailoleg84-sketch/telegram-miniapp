# Аудит улучшений приложения

> Дата: 2026-06-15. Многоагентный аудит (8 измерений, 72 находки), сведённый в приоритизированный бэклог. Связано с [[04 План задач]] · [[03 Технический аудит]] · [[06 Журнал изменений]].
> Формат: **impact** (high/med/low) · **effort** (S/M/L) · файл:строка.

## 🔴 Срочно — безопасность детей и расходы

- [x] ✅ **Чистка банка слов 5–7** (выполнено 2026-06-15): мусорные аббревиатуры, `bucks/bar/bars`, `boyfriend/girlfriend` → в `BLOCKED_SEED_WORDS`; исправлены переводы `the/an/am/afternoon/ash/arms/branches`; слур нейтрализован. Слур/оружие/сленг уже были заблокированы ранее. Тест-страж `tests/test_word_safety.py`. (high·S)
- [x] ✅ **Глобальный потолок расходов OpenAI** (2026-06-15): `OPENAI_DAILY_COST_LIMIT_USD` + `database.get_ai_cost_today_total` + `_ai_budget_exceeded` (log.critical, fail-open) в чат/голос/Realtime/TTS/картинках. Активируется заданием env (по умолчанию 0). Тест `tests/test_ai_budget.py`. (Per-user `AI_DAILY_MESSAGE_LIMIT` оставлен как продуктовый рычаг владельца.)
- [x] ✅ **Webhook-секрет — ложная тревога:** `main.py:58-65` уже делает fail-fast (`sys.exit`) в webhook-режиме при пустом секрете. В проде секрет обязателен. Менять не нужно.
- [x] ✅ **Контент-фильтр TTS** (2026-06-15): курированный `_TTS_BLOCKED_RE` (мат/оскорбления/18+/слёрсы, границы слова) в `api_audio_speech` до кэша → 400. Исключены контекстно-нормальные учебные слова; убран ложный позитив `pissed`. Adversarial-проверка: нет обходов, фронт держит 400, нет ложных блокировок реального словаря. Тест `tests/test_tts_safety.py`. (med·M)
- [ ] Safety-guard смотрит только последнюю реплику (openai_service ~578) — многошаговую инъекцию не ловит. Сканировать 2–3 последних user-хода для wants_secret/wants_prompt. (med·M)
- [ ] Имя ребёнка уходит в каждый промпт OpenAI (COPPA/GDPR) — ограничить 20 симв., только имя/ник. (low·S)
- [ ] Осиротевшая колонка `parent_pin_hash` в проде — `ALTER TABLE users DROP COLUMN IF EXISTS` в миграции. (low·S)

## 🟢 Быстрые победы (high impact · S)

- [x] ✅ **Тематические колоды слов** (2026-06-15): data-driven пикер — `GET /api/vocab/topics` (темы с ≥6 слов для возраста, метка+эмодзи) → `renderVocabStart` показывает плитки тем + «Любые слова», `renderVocabWords(topic)` грузит набор. `database.get_topic_counts`.
- [x] ✅ **Тач-зона кнопки голоса** (2026-06-15): базовые правила `.voice-mode-toggle` 42→44px. (Реальная чат-кнопка `.chat-wrap .voice-mode-toggle` была уже 48px — находка аудита оказалась ложной, как и слур; базу выровнял для инварианта.)
- [x] ✅ **Loading при регистрации** (2026-06-15): кнопка дизейблится + «Создаём профиль…» на время запроса (анти-дабл-тап).
- [x] ✅ **`/readyz` с пингом БД** (2026-06-15): `database.ping()` + эндпоинт (503 если БД недоступна). `healthCheckPath` оставлен `/healthz` (liveness) — чтобы холодный старт Neon не вызывал флаппинг на free-плане; `/readyz` для мониторинга/мульти-инстанса.
- [x] ✅ **`auth.py` тесты** (2026-06-15): `tests/test_auth.py` — валид/подделка/протух/нет полей для fallback и initData.
- [x] ✅ **Эмодзи для старших** (2026-06-15): +55 проверенных эмодзи в `WORD_EMOJI` (16 целевых 11–18 + 39 кросс-возрастных пропусков); банк ~320→375. Метод: пересечение курированного словаря с реальным банком + сверка с переводом. Adversarial-ревью поймало коллизии (chef/baker, certificate/history, newspaper/magazine). `tests/test_word_emoji.py`. NB: банк 11–18 абстрактный — качественных кандидатов десятки, не «80–100» (паддинг неоднозначными глифами вреден). (high·M)

## 🟡 Стратегические

### Производительность
- [x] ✅ **`asyncio.gather` на тяжёлых эндпоинтах** (2026-06-15): `api_parent_report` (5), `api_motivation_status` (4), `api_learning_path` (4), `api_admin_user_detail` (7) — независимые запросы параллельно после гейта пользователя (×3–5 латентность). Дедлока нет (нет вложенного захвата; пул max_size=5). (med·S)
- [ ] Полный ре-сид 5000 слов на каждом старте (`_seed_words`) → хеш-гард, сеять только при изменении. (med·M)
- [ ] `ORDER BY RANDOM()` ×12 (database.py) → TABLESAMPLE / offset-трюк / кэш id по age_group. (med·M)
- [ ] `get_words_for_age` до 3 запросов каскадом → один `ORDER BY CASE topic …`. (med·S)
- [ ] S3Storage создаёт новый клиент на каждый read/write (storage.py ~117) → кэшировать клиент. (med·M)
- [ ] `add_message`/`issue_training_token`/`get_learning_streak` — лишние запросы/выгрузки → CTE/окно/фон. (low·S)
- [ ] Индекс `words(age_group, topic)`. (low·S)
- [ ] gpt-image-1: до 4 вызовов на слово (generate+vision×retry) → 0 ретраев по умолчанию, пропуск vision для object/action. (high·M)

### Архитектура / код
- [ ] Протечка слоёв: `database.py` импортирует `webapp.vocabulary_visualizer` (database.py:14) → вынести build_visual из `_seed_words`. (med·S)
- [ ] Нет сервисного слоя: ~127 прямых `database.*` из роутов. (high·L)
- [ ] `app.js` монолит 4862 строки (renderChat ~1700) → ES-модули (audio/chat/admin/vocab). (high·L)
- [ ] `init_db`: 29 ALTER + сид НЕ в транзакции, без версий миграций → транзакция + `schema_versions`. (high·M)
- [ ] Config sprawl: ~14 прямых `os.getenv` мимо config.py. (low·S)
- [ ] Хардкод `?v=20260606-...` в app.js (≠ APP_VERSION) → инжектить `__APP_VERSION__`. (low·S)

### Надёжность / ops
- [ ] Авто-деплой в прод без стейджа/пред-деплой-тестов → `preDeployCommand: pytest` в render.yaml. (med·M)
- [ ] DB-пул без таймаутов (database.py create_pool) → `command_timeout`, `connection_timeout`. (med·S)
- [ ] Нет graceful-shutdown (in-flight запросы рвутся на SIGTERM) → `AppRunner(shutdown_timeout=15)` + on_shutdown. (med·M)
- [ ] Логи плоские, без LOG_LEVEL/структуры → JSON-формат + user_id/latency/path. (med·S)
- [ ] Rate-limit падает в in-memory при сбое Redis (мульти-инстанс обходится) → требовать REDIS_URL / fail-closed для AI. (med·S)
- [ ] Админ-обзор только за сегодня → 7-дн агрегат расходов. (low·S)

### Удержание / продукт
- [x] ✅ **Напоминания ботом** (2026-06-15): opt-in, внешний GitHub Actions cron (~18:00 МСК) → защищённый `/internal/send-reminders` (обходит спячку Render free). `webapp/reminders.py` + колонки `reminders_enabled`/`last_reminded_at` + тумблер в Настройках. Идемпотентно (1/день), стрик-текст, авто-выкл заблокировавших. Adversarial-ревью пройдено. `tests/test_reminders.py`. ⚠️ Активация: задать `REMINDER_CRON_SECRET` (Render + GitHub Secrets) + включить тумблер. (high·M)
- [~] 🟡 **Недельный отчёт родителю** (2026-06-15): in-app срез «За 7 дней» готов (`get_parent_report_week` + карточка в отчёте; дней-активности/уроки/тесты+средний/игры/слов отработано). Adversarial-ревью SQL+фронт пройдено. Осталось опционально: рассылка отчёта по воскресеньям ботом (переиспользует cron-инфраструктуру напоминаний). (high·M)
- [ ] Активация фритир-лимита + CTA на подписку (инфраструктура лимитов уже есть). (med·S)
- [x] ✅ **Достижения: вехи + анимация** (2026-06-15): лестница бейджей 8→18 (серия 14/30, уроки 10/30, слова 100/250, верные 100/250, тесты 10, игры 10); анимация разблокировки (`diffNewlyUnlocked` + localStorage, первый заход тихо) + лента «Новое!» + haptic. `tests/test_motivation_badges.py`. (med·M)
- [ ] Лидерборд: недельный + по возрастным группам (сейчас all-time общий). (med·M)
- [ ] Воронка-аналитика: таблица `events` (open/register/lesson_start/abandon) — сейчас решения вслепую. (med·M)
- [ ] Предгенерация картинок частотного ядра (~300–500 слов, ~$10 разово). (med·L)

### Тесты / качество
- [ ] `test_openai_safety` — хрупкий «оракул по тексту» (242 ассерта на подстроки) → разбить + AST для маршрутов. (high·M)
- [ ] Нет тестов: `auth.py`, `svg_renderer.py`, `_looks_like_audio`, `payload_builders.py`. (high/med·S)
- [ ] Нет интеграции против реальной БД (FakePool не парсит SQL) → `testcontainers` + критичные write-пути. (high·L)
- [ ] CI только `node --check` для app.js → JSDOM smoke на чистые функции. (high·L)
- [ ] Нет замера покрытия → `pytest-cov --cov-fail-under`. (med·S)

### Контент / данные
- [~] 🟡 **Реклассификация тем — консервативный проход** (2026-06-15): курированный `_TOPIC_OVERRIDE` (92 сверенных переезда в темы-колоды; источник не тронут, обратимо). Adversarial-ревью APPROVE. `tests/test_topic_reclassify.py`. NB: находка про «плохие дистракторы» оказалась ложной — они случайны (`random.sample`), тема не участвует; реальная польза — колоды + визуал object vs situation. Осталось опционально: агрессивный разбор остатка `everyday`/`verbs` (выше риск). (med·M/L)
- [ ] Примеры для ~75% слов — учебные фреймы (gap-fill тривиален) → расширить курируемые наборы для частотного ядра. (high·L)
- [ ] Level-test 5–8 вопросов на группу → расширить до 12–15, добавить продуктивные. (med·M)
- [ ] `topic_plans` тонкие (3 слова/тема, нет грамматики/диалога) → углубить. (med·L)
- [ ] 6 слов из topic_plans отсутствуют в банке (doll/sunny/rainy/playlist/luggage/booking) → добавить. (med·S)
