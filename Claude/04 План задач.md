# План задач

> Приоритезированный бэклог. Связано с [[03 Технический аудит]] · [[05 Первые задачи]].
> Дата: 2026-06-08. Детальные промты под каждую задачу — в `Claude/План задач для Codex.md`.
> ✅ — уже сделано и задеплоено (Этап 1 + авто-возраст).

## Критичные

- [x] ✅ Обязательный webhook-секрет (fail-fast без `TELEGRAM_WEBHOOK_SECRET`)
- [x] ✅ Суточный лимит на Realtime-сессии
- [x] ✅ Индексы БД на горячих таблицах
- [x] ✅ CSP-заголовок + валидация типа аудио
- [x] ✅ **Persistent storage для кэшей картинок/аудио/фото** (инкремент 1 — 2026-06-09)
  - сделано: слой `webapp/storage.py` (LocalDiskStorage + интерфейс) + конфигурируемый `CACHE_ROOT`; раздача картинок из CACHE_ROOT, если он вынесен за static; тесты `tests/test_storage.py`
  - как включить персистентность: на Render подключить persistent disk и задать `CACHE_ROOT` на точку монтирования (кэши переживут деплой; новых зависимостей не нужно)
  - [x] ✅ инкремент 2 (2026-06-09): S3/R2 backend для картинок слов. `S3Storage` + `make_storage` (выбор по `R2_*`); генерация/существование/раздача проведены через async-слой; S3 — прокси-раздача, доверие БД (персистентно).
  - [x] ✅ инкремент 3 (2026-06-09): озвучка (TTS) через R2 тем же способом. `word_audio_storage = make_storage("audio")`; `_word_audio_cache_name` (голое имя), чтение/запись/eviction через storage; админ-сводка терпит S3 (`None`). Тесты `tests/test_audio_cache.py`. **158 passed.**
  - [x] ✅ инкремент 4 (2026-06-09): фото Pixabay через R2. `vocab_photo_storage = make_storage("vocab_photos")`; `_vocab_photo_cache_name` (голое имя), чтение фото/`.none`-маркера и запись через storage; отдача инлайн (без статик-URL/БД). Тесты `tests/test_vocab_photo_cache.py`. **162 passed.**
  - **чтобы включить:** создать Cloudflare R2 bucket + API-токен, задать в Render `R2_BUCKET`, `R2_ACCOUNT_ID` (или `R2_ENDPOINT_URL`), `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY` (одни и те же ключи для всех трёх кэшей)
  - **готово:** все три файловых кэша (картинки, озвучка, фото) переведены на R2; в дефолте без `R2_*` — локальный диск, как раньше.
  - риск: 🟡 средний (в дефолте поведение прежнее)
- [x] ✅ **Вынос состояния в Redis (rate-limit, токены тренировок)** (2026-06-09)
  - `webapp/redis_store.py` (rate_limit_ok fixed-window + issue/consume_token GETDEL, ленивый `redis`); `rate_limiter` async-диспетчеры; токены тренировок → async + диспетчер. Фолбэк на in-memory при ошибке/без `REDIS_URL`.
  - **чтобы включить:** Upstash Redis → задать `REDIS_URL` в Render
  - осталось опц.: rate-limit на sliding-window вместо fixed (точнее), общий счётчик расходов
- [x] ✅ **Персистентность токенов тренировок через Neon (Upstash не нужен)** (2026-06-09)
  - токены критичны к рестарту → по умолчанию хранятся в **уже подключённом Neon**: таблица `training_tokens` (создаётся в `init_db()`), `issue_training_token`/`consume_training_token` (атомарный `DELETE … RETURNING`). Порядок бэкендов: Redis → **Neon (дефолт)** → in-memory.
  - rate-limit оставлен in-memory (не критичен к персистентности; БД-запрос на каждый HTTP — лишняя нагрузка).
  - **чтобы включить:** ничего — таблица создастся сама на ближайшем деплое; Redis остаётся опциональным.
  - тесты: `TrainingTokenSqlTests` (FakePool, не трогает прод-БД); **153 passed**.

## Важные

- [x] ✅ **Устранить N+1 в квизах и тренировках** (2026-06-09)
  - сделано: пул дистракторов одним запросом (`_pick_distractors`); `update_progress_bulk` (executemany); билдеры стали чистыми
  - файлы: `webapp/server.py`, `database.py`, тесты `tests/test_quiz_building.py`
  - результат: 1+N → 2 запроса на сессию; 73 теста passed
- [x] ✅ **Кэш `index.html` в памяти + gzip/Cache-Control статики** (2026-06-08; версионирование `?v=` уже было)
  - файлы: `webapp/server.py`
  - риск: 🟢 низкий
- [x] ✅ **Параллелизация независимых DB-вызовов в голосовом/чат-ходе** (2026-06-09)
  - сделано: `asyncio.gather` для гейт+профиль, запись реплики ∥ продвижение урока (разные таблицы)
  - файлы: `webapp/server.py` (`api_chat_send`, `_voice_text_turn_payload`), тест `tests/test_voice_turn.py`
  - результат: меньше латентности на голосовой ход; 75 тестов passed
- [x] ✅ **Тесты: интеграционные (HTTP/auth/rate-limit) + покрытие `database.py`** (2026-06-09)
  - `tests/test_server_integration.py` — auth/заголовки/статика/rate-limit
  - `tests/test_database.py` — фейковый asyncpg-пул: ветки SQL, порядок параметров, маппинг (update_progress/bulk, usage, get_random_words, add_user, …)
  - стратегия: мок-уровень (без инфраструктуры, не трогает прод-БД); SQL-корректность против Postgres — отдельный интеграционный уровень при наличии тестовой БД
- [x] ✅ **Дедуп выборки слова + единая «лестница» возраста** (2026-06-09)
  - `_select_training_word` — общий каскад для `api_choice_next`/`api_input_next` (тест `tests/test_training_select.py`)
  - «лестница» возраст→группа сведена в `config.age_group_from_age` (единый источник); server и openai_service делегируют туда
  - сознательно НЕ слиты целиком: learning-режим (приоритет сохранённой группы) и voice-режим (приоритет точного возраста) — разная семантика, зафиксирована тестами `tests/test_age_group.py`
  - осталось опционально: дедуп подсчёта в finish-обработчиках (разные модели очков — низкая ценность)
- [x] ✅ **Усилить safety-guard и фильтрацию промпт-инъекций** (2026-06-09)
  - сделано: «сжатая» нормализация (lower+leet+удаление разделителей) ловит обходы; `neutralize_injection` обезвреживает служебные токены в истории для модели
  - файлы: `webapp/openai_service.py`, тест `tests/test_safety_guard.py`
  - результат: ловит «а-п-и ключ», «0penai key», «i g n o r e previous», `<|system|>`; 86 тестов passed

## Можно позже

- [~] Разбить `server.py`/`app.js` на модули + сервисный слой (после тестов) — поэтапно
  - [x] ✅ шаг 1: SVG-рендер → `webapp/svg_renderer.py` (2026-06-09, server.py −599 строк, re-export 3 функций)
  - [x] ✅ шаг 2: rate-limiter → `webapp/rate_limiter.py` (2026-06-09, server.py −65 строк, re-export 4 имён)
  - [ ] далее: `payload_builders`, `routes/{chat,training,admin}`, сервисный слой
- [~] Единый источник истины для слов; живые примеры; интервальное повторение (SRS)
  - [x] ✅ SRS-движок (2026-06-09, Leitner): `srs_box`+`next_review_at` в `user_progress`, обновление прогресса + подбор `get_review_word` по «пора повторить», идемпотентная миграция старых строк. **Счётчики словаря/сводки (`get_user_dictionary`/`get_dictionary_summary`/`needs_review`) выровнены на тот же SRS-источник** (адверсариальная проверка нашла рассинхронизацию — устранена). Тесты в `tests/test_database.py`. **168 passed.**
  - [~] SRS-история для пользователя (обзор 23 точек через workflow):
    - [x] ✅ шаг 1 сервер (2026-06-09): приоритет «повторить»>«выучено» в `_dictionary_word_dict`; SRS-формулировки в learning_path/motivation/parent_recommendations. Тест в `test_openai_safety`. **169 passed.**
    - [ ] шаг 2 фронт: счётчик «N готово к повторению» (карточка/тайл); чипы статуса в словаре + рабочий фильтр (`word.status_label` уже приходит).
  - [ ] follow-up (опц.): `CREATE INDEX CONCURRENTLY` вручную при росте таблицы; разбивка «выучено, но пора освежить» родителю/админу; живые примеры; единый источник слов
- [x] ✅ Общий таймаут Realtime + удешевление «русского ремонта» (2026-06-09)
  - таймаут: `api_realtime_token` обёрнут в `asyncio.wait_for(REALTIME_TOKEN_TIMEOUT_SEC=30)` → дружелюбный 504 вместо ~50с зависания; per-attempt 25→15с (retry влезает в бюджет)
  - двойной вызов: НЕ удалён (это safety-net качества русской речи, срабатывает только на плохом ответе, покрыт тестом) — вместо этого сделан дешевле (low reasoning/temp)
  - тест: `test_realtime_token_timeout_returns_friendly_504`
- [x] ✅ Ретенция `conversations` (последние N=50 на пользователя, чистка при записи) — 2026-06-09
- [x] ✅ Вынос `LEVEL_TESTS`/`TOPIC_PLANS` в `data/` (2026-06-09) — `data/level_tests.py`, `data/topic_plans.py`; lesson_engine реэкспортирует TOPIC_PLANS
- [x] ✅ Чистка временных файлов и проверка `.gitignore` (2026-06-09) — репозиторий уже чистый (логи/`.pid`/`tmp` не отслеживаются); добавлен `.pytest_cache/` в `.gitignore`
