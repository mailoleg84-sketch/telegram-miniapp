---
type: reference
project: AI English Tutor Kids
date: 2026-06-08
status: backlog
tags: [codex, tasks, backlog, plan]
---

# 🧩 План задач для Codex

> Подготовлено по правилам №3 и №6 из [[00_Правила для Claude]] на основе [[Технический аудит]] и карты проекта.
> Каждая задача — самостоятельный промт для Codex. Код здесь **не менялся**.

## Как пользоваться

1. Бери одну задачу за раз, начиная с блока **1. Срочно исправить**.
2. Копируй блок «Промт для Codex» целиком.
3. Во всех промтах действует общее ограничение — **не трогать `.env`, ключи, токены, секреты; не удалять файлы без подтверждения; сохранять обратную совместимость API и поведение для пользователя**.
4. После каждой задачи прогоняй проверки:
   ```bash
   .venv\Scripts\python.exe -m unittest discover -s tests -v
   .venv\Scripts\python.exe -m py_compile config.py database.py webapp\server.py webapp\openai_service.py webapp\lesson_engine.py
   node --check webapp\static\app.js
   ```

### Легенда приоритетов
🔴 критично · 🟠 высокий · 🟡 средний · 🟢 низкий

---

## 1. 🚨 Срочно исправить

### 1.1 🔴 Обязательный секрет Telegram webhook
- **Что сделать:** сделать `TELEGRAM_WEBHOOK_SECRET` обязательным в webhook-режиме: при пустом значении — fail-fast на старте с понятной ошибкой. Убрать молчаливый `secret_token=... or None`.
- **Файлы:** `main.py:37-69`, `config.py:32`, `webapp/server.py:3792`.
- **Ожидаемый результат:** в проде бот не стартует без секрета; поддельные апдейты на `/telegram/webhook` отклоняются Telegram-проверкой.
- **Риск:** низкий. Нужно не сломать локальный polling-режим (там секрет не нужен).
- **Промт для Codex:**
  > В проекте Telegram Mini App (Python, aiogram+aiohttp) webhook не защищён: `TELEGRAM_WEBHOOK_SECRET` по умолчанию пустой (`config.py:32`), а `main.py:69` и `webapp/server.py:3792` передают `secret_token=TELEGRAM_WEBHOOK_SECRET or None`, что отключает проверку. Сделай так: в `main.py` при `BOT_RUN_MODE == "webhook"` и пустом `TELEGRAM_WEBHOOK_SECRET` — печатать понятную ошибку в stderr и `sys.exit(1)` (как уже сделано для `BOT_TOKEN`/`DATABASE_URL`). В polling-режиме проверку не требовать. Не меняй сигнатуры функций без необходимости, сохрани локальный запуск. Не трогай `.env`. Прогони `python -m py_compile main.py config.py webapp/server.py`.

### 1.2 🔴 Суточный лимит на дорогие Realtime-сессии
- **Что сделать:** ввести отдельный жёсткий суточный лимит на выдачу Realtime-токенов (не зависящий от общего `AI_DAILY_MESSAGE_LIMIT`), напр. `REALTIME_DAILY_SESSION_LIMIT`. Считать выданные токены per-user в БД.
- **Файлы:** `webapp/server.py:3589-3619`, `config.py:88-97`, `database.py` (счётчик использования), `webapp/openai_service.py:1157`.
- **Ожидаемый результат:** один аккаунт не может нажечь много $ на Realtime; при превышении — корректный 429 с сообщением.
- **Риск:** средний — нужно не сломать обычный голосовой сценарий; выбрать вменяемый дефолт (напр. 40 сессий/сутки).
- **Промт для Codex:**
  > В `webapp/server.py` обработчик выдачи Realtime-токена (`api_realtime_token`, около строки 3589) создаёт платную сессию OpenAI (~$0.05). Сейчас защита только через `AI_RATE_LIMIT_PER_MINUTE`, а `AI_DAILY_MESSAGE_LIMIT` по умолчанию 0 (выключен). Добавь отдельный суточный лимит на Realtime-токены: новая настройка `REALTIME_DAILY_SESSION_LIMIT` в `config.py` (дефолт 40, 0 = выкл), подсчёт выданных за сутки токенов per-user через функцию в `database.py` (по аналогии с `get_ai_usage_today`/`add_ai_usage`), и проверка в обработчике перед выдачей — при превышении вернуть `web.json_response({"error": ...}, status=429)`. Не меняй формат успешного ответа. Прогони тесты и py_compile.

### 1.3 🔴 Индексы БД на горячих таблицах
- **Что сделать:** добавить недостающие индексы в `init_db()` (idempotent `CREATE INDEX IF NOT EXISTS`).
- **Файлы:** `database.py:180-260` (рядом с существующими индексами).
- **Ожидаемый результат:** нет seq scan на `conversations`, `user_progress`, `daily_lessons`, `words(age_group)`; стабильная скорость при росте.
- **Риск:** очень низкий (только добавление индексов, idempotent).
- **Промт для Codex:**
  > В `database.py` функция `init_db()` уже создаёт индексы для `ai_usage`, `vocabulary_sessions`, `game_sessions`, `training_attempts` (строки ~186-249). Добавь рядом ещё четыре idempotent-индекса:
  > ```sql
  > CREATE INDEX IF NOT EXISTS conversations_user_id_idx ON conversations (user_id, id DESC);
  > CREATE INDEX IF NOT EXISTS user_progress_user_id_idx  ON user_progress (user_id);
  > CREATE INDEX IF NOT EXISTS daily_lessons_user_id_idx  ON daily_lessons (user_id);
  > CREATE INDEX IF NOT EXISTS words_age_group_idx        ON words (age_group);
  > ```
  > Проверь по реальным `CREATE TABLE` в этом же файле, что имена таблиц и колонок совпадают (`user_id`, `age_group`). Ничего не удаляй. Прогони py_compile.

### 1.4 🟠 CSP-заголовок и валидация типа аудио
- **Что сделать:** добавить `Content-Security-Policy` в `hardening_middleware`; проверять magic bytes загружаемого аудио перед отправкой в Whisper.
- **Файлы:** `webapp/server.py:3703-3723` (middleware), `webapp/server.py:1092-1125` (загрузка аудио).
- **Ожидаемый результат:** базовая защита от инъекции скриптов; отсев заведомо неаудио-файлов.
- **Риск:** средний — слишком строгий CSP может сломать Telegram WebView/инлайн-стили; начать с мягкой политики.
- **Промт для Codex:**
  > Две правки в `webapp/server.py`. (1) В `hardening_middleware` (~3703-3723), где уже ставятся `X-Content-Type-Options` и `Referrer-Policy`, добавь заголовок `Content-Security-Policy`. Mini App работает внутри Telegram WebView и использует инлайн-стили, поэтому начни с безопасной, но не ломающей политики: `default-src 'self'; img-src 'self' data: https:; style-src 'self' 'unsafe-inline'; script-src 'self'; connect-src 'self' https: wss:; object-src 'none'`. Не выставляй `X-Frame-Options` (приложение — iframe). (2) В обработчике загрузки аудио (~1092-1125) перед отправкой в Whisper проверь первые байты буфера на сигнатуры WebM (`\x1a\x45\xdf\xa3`), OGG (`OggS`), MP3 (`ID3` или `\xff\xfb`), WAV (`RIFF`); при несовпадении вернуть 400. Лимит размера 8 МБ не трогай. Прогони тесты и py_compile.

---

## 2. 🎨 Улучшить интерфейс

### 2.1 🟡 gzip-сжатие и кэширование статики
- **Что сделать:** включить сжатие ответов и `Cache-Control` для статики; версионировать `app.js`/CSS через `?v=APP_VERSION`.
- **Файлы:** `webapp/server.py:3731-3798` (`create_app`, `add_static`), `webapp/static/index.html`, `config.py:45` (`APP_VERSION`).
- **Ожидаемый результат:** `app.js` (196 КБ) едет ~50–60 КБ; кэш в браузере; быстрее загрузка на мобильном.
- **Риск:** низкий. Следить, чтобы версионирование инвалидировало кэш при деплое.
- **Промт для Codex:**
  > В aiohttp-приложении (`webapp/server.py`) статика отдаётся через `add_static` без сжатия, `app.js` весит ~196 КБ. Включи gzip-сжатие текстовой статики (можно middleware, проверяющий `Accept-Encoding: gzip` и сжимающий ответы для `.js/.css/.html/.svg`, либо аккуратно через `Content-Encoding`), добавь `Cache-Control: public, max-age=86400` для `/static/*`. В `webapp/static/index.html` добавь к подключению `app.js`, `styles.css`, `design.css` query-версию `?v=` со значением `APP_VERSION` (пробрось его в шаблон index_handler). Не ломай отдачу `index.html`. Прогони `node --check webapp/static/app.js` и py_compile.

### 2.2 🟡 Лоадеры и обработка ошибок сети на фронте
- **Что сделать:** единый UX для долгих операций (генерация картинки до 75 с, Realtime-инициализация до 50 с): спиннер/прогресс + дружелюбное сообщение при ошибке/таймауте, без «зависшего» экрана у ребёнка.
- **Файлы:** `webapp/static/app.js`, `webapp/static/design.css`/`styles.css`.
- **Ожидаемый результат:** на каждом долгом действии понятная индикация и graceful-ошибка.
- **Риск:** средний — большой монолитный `app.js`, легко задеть смежную логику. Менять точечно.
- **Промт для Codex:**
  > Во фронтенде `webapp/static/app.js` (vanilla JS) у долгих операций (генерация картинки слова, инициализация голосового Realtime, отправка сообщения) нет единообразной индикации загрузки и обработки таймаутов. Добавь переиспользуемый помощник показа/скрытия лоадера и единую обработку ошибок fetch (сеть/таймаут/не-2xx) с дружелюбным детским сообщением вместо застывшего экрана. Используй существующую функцию `esc()` для экранирования. Не меняй контракт API и формат запросов. Стили лоадера добавь в `design.css`. Прогони `node --check webapp/static/app.js`.

### 2.3 🟢 Аудит доступности/мобайла нового дизайна
- **Что сделать:** проверить новый дизайн (нижняя навигация, фиолетовая тема) на контраст, размеры тач-зон (≥44px), безопасные зоны iOS, читаемость на маленьких экранах.
- **Файлы:** `webapp/static/design.css`, `styles.css`, `index.html`, `app.js` (навигация).
- **Ожидаемый результат:** список конкретных правок + сами правки CSS без регрессий.
- **Риск:** низкий.
- **Промт для Codex:**
  > Проверь новый дизайн Mini App (`webapp/static/design.css`, `styles.css`, фиксированная нижняя навигация) на мобильную доступность: контраст текста на фиолетовой теме (WCAG AA), тач-зоны кнопок навигации ≥44×44px, учёт `env(safe-area-inset-bottom)` для iOS, отсутствие горизонтального скролла на 320px. Внеси точечные CSS-правки, не меняя структуру разметки и JS. Перечисли, что исправил.

---

## 3. 🛡️ Улучшить стабильность

### 3.1 🔴 Persistent storage для кэшей картинок/аудио
- **Что сделать:** перенести кэш сгенерированных картинок, TTS и Pixabay-фото с эфемерного диска Render на постоянное хранилище (Render persistent disk или S3/Cloudflare R2).
- **Файлы:** `webapp/server.py:81-172` (пути кэша, eviction), `webapp/openai_service.py` (генерация), `database.py:632-651` (URL картинок), `render.yaml`.
- **Ожидаемый результат:** кэш переживает деплой; нет повторной оплаты OpenAI за уже сгенерированное.
- **Риск:** высокий — затрагивает пути, выдачу файлов и согласованность с URL в БД. Делать с фолбэком и поэтапно.
- **Промт для Codex:**
  > Кэши TTS (`AUDIO_CACHE_DIR`), сгенерированных картинок (`STATIC_DIR/generated/...`) и Pixabay-фото лежат на эфемерном диске Render и стираются при каждом деплое, хотя URL картинок сохранён в БД (`database.py:632-651`) — это приводит к регенерации за деньги. Спроектируй и реализуй слой хранения с двумя бэкендами за единым интерфейсом (`get/put/exists/url`): локальный диск (как сейчас, для dev) и S3-совместимый (Cloudflare R2) для прод, выбор по env-переменным (имена задокументируй в `.env.example`, **сам `.env` не трогай**). Замени прямые обращения к файловому кэшу в `webapp/server.py` и `webapp/openai_service.py` на этот слой. Сохрани обратную совместимость: если облако не настроено — работать с локальным диском как раньше. Прогони тесты и py_compile.

### 3.2 🟠 Redis для rate-limit и токенов тренировок
- **Что сделать:** вынести `_rate_buckets`, `_photo_ip_buckets`, `_training_attempts` из памяти процесса в Redis (Upstash) с фолбэком на in-memory, если Redis не настроен.
- **Файлы:** `webapp/server.py:101-155, 3033-3062`, `config.py`, `requirements.txt`.
- **Ожидаемый результат:** rate-limit не обходится при нескольких инстансах; токены тренировок переживают рестарт.
- **Риск:** средний — новая зависимость и сетевые вызовы в горячем пути; обязательно фолбэк.
- **Промт для Codex:**
  > Сейчас rate-limit (`_rate_buckets`, `_photo_ip_buckets` в `webapp/server.py:101`) и одноразовые токены тренировок (`_training_attempts`, `webapp/server.py:3033`) хранятся в памяти процесса — ломаются при рестарте и при нескольких инстансах. Введи абстракцию хранилища с двумя реализациями: in-memory (текущее поведение, дефолт) и Redis (по env `REDIS_URL`). Используй её в rate-limiter и в выдаче/проверке токенов тренировок. Если `REDIS_URL` не задан — поведение строго как сейчас. Добавь зависимость в `requirements.txt`, env-переменную в `.env.example` (не трогай `.env`). Прогони тесты (включая тест одноразовых токенов в `tests/test_openai_safety.py`) и py_compile.

### 3.3 🟡 Единая обработка ошибок и устранение молчаливого глотания
- **Что сделать:** причесать обработку ошибок: `_safe_json` должен логировать битый JSON; единый хелпер для ответов об ошибке.
- **Файлы:** `webapp/server.py:1087, 411, 2718`.
- **Ожидаемый результат:** меньше «немых» падений, единообразные ответы.
- **Риск:** низкий.
- **Промт для Codex:**
  > В `webapp/server.py` функция `_safe_json` (~1087) молча возвращает `{}` при битом JSON без лога. Добавь `log.warning` с путём запроса. Пройди по другим `except Exception: pass`/без лога (например ~411, ~2718) и добавь логирование, не меняя поток управления. Не вводи новых ответов клиенту. Прогони py_compile.

---

## 4. ⚡ Улучшить скорость

### 4.1 🟠 Устранить N+1 в квизах и тренировках
- **Что сделать:** заменить пословные запросы дистракторов и пословный `update_progress` на батч-операции.
- **Файлы:** `webapp/server.py:2841-2845, 2887-2916, 2950, 2980-3000`, `database.py:658-766` (выборка слов), функции `update_progress`.
- **Ожидаемый результат:** −0.5…1 сек на сессию квиза; меньше нагрузки на Neon.
- **Риск:** средний — менять логику формирования вопросов аккуратно, сохранить рандомизацию и отсутствие дублей-дистракторов.
- **Промт для Codex:**
  > В `webapp/server.py` есть N+1 к БД: `api_vocab_quiz` (~2841) и `api_word_hunt_start` (~2950) вызывают выборку дистракторов на каждое слово; `api_vocab_finish` (~2903) и `api_word_hunt_finish` (~3000) делают `update_progress` в цикле. Оптимизируй: (1) для дистракторов — одной выборкой получить пул случайных слов нужной возрастной группы (`SELECT ... WHERE age_group=$1 ORDER BY RANDOM() LIMIT N`) и раздать их вопросам в Python, исключая правильный ответ и дубли; (2) для прогресса — добавить в `database.py` батч-функцию (`executemany`) и вызвать её один раз. Сохрани формат ответов, рандомизацию и защиту от повторов. Прогони тесты и py_compile.

### 4.2 🟡 Параллелизация независимых DB-вызовов в голосовом ходе
- **Что сделать:** в `_voice_text_turn_payload` и `api_chat_send` собрать независимые чтения через `asyncio.gather`; запись `add_ai_usage` — фоном.
- **Файлы:** `webapp/server.py:3251-3308, 3406-3459`.
- **Ожидаемый результат:** меньше latency на голосовой/чат-ход.
- **Риск:** средний — следить за зависимостями по данным (что от чего зависит), не распараллелить зависимые шаги.
- **Промт для Codex:**
  > В `webapp/server.py` обработчики `api_chat_send` (~3251) и `_voice_text_turn_payload` (~3406) выполняют ~10 последовательных await к БД и OpenAI. Распараллель независимые чтения (`get_ai_usage_today`, `get_user`, `get_recent_messages` — те, что не зависят друг от друга) через `asyncio.gather`, сохраняя строгий порядок там, где есть зависимость по данным (сначала `add_message`, потом advance state). Запись метрики `add_ai_usage` можно выполнять после формирования ответа. Не меняй контракт ответа. Прогони тесты и py_compile.

### 4.3 🟡 Не читать index.html с диска на каждый запрос
- **Что сделать:** кэшировать содержимое `index.html` в памяти при старте (с учётом подстановки `APP_VERSION`).
- **Файлы:** `webapp/server.py:3650-3660`.
- **Ожидаемый результат:** нет блокирующего синхронного I/O в event loop на каждый заход.
- **Риск:** низкий — учесть, что в dev удобно видеть правки без рестарта (сделать опционально).
- **Промт для Codex:**
  > `index_handler` в `webapp/server.py` (~3651) читает `index.html` синхронно `read_text()` на каждый запрос — блокирует event loop. Закэшируй содержимое в памяти при старте приложения (в `create_app`/on_startup), отдавай из кэша. Если в файле подставляется `APP_VERSION` — выполнить подстановку один раз. Для dev можно оставить флаг перечитывания по env, по умолчанию — кэш. Прогони py_compile.

### 4.4 🟡 Не блокировать event loop тяжёлым CPU при сидинге
- **Что сделать:** вынести `build_vocabulary_visual` (×5000) из синхронного цикла в `asyncio.to_thread` или предвычислять визуалы вне старта.
- **Файлы:** `database.py:267-353`, `webapp/vocabulary_visualizer.py`.
- **Ожидаемый результат:** быстрый и неблокирующий старт; укладываемся в лимит запуска Render.
- **Риск:** средний — менять путь сидинга; убедиться, что результат идентичен.
- **Промт для Codex:**
  > В `database.py` функция `_seed_words` (~267-353) синхронно вызывает `build_vocabulary_visual` для ~5000 слов в async-контексте при каждом старте, блmy event loop. Перенеси тяжёлое вычисление визуалов в поток через `asyncio.to_thread` (или собери список в отдельной синхронной функции и вызови её одним `to_thread`). Результат сидинга должен остаться идентичным. Также рассмотри запуск сида только при пустой/устаревшей таблице. Прогони тесты и py_compile.

---

## 5. 📚 Улучшить обучение словам

### 5.1 🟠 Единый источник истины для слов и переводов
- **Что сделать:** устранить рассинхрон `data/words.py` ↔ `data/single_words_5000.py`; собрать переводы из одного места.
- **Файлы:** `data/words.py`, `data/single_words_5000.py`, `database.py:_seed_words`, `tools/relabel_words.py`.
- **Ожидаемый результат:** правка перевода в одном месте; нет противоречий между банками.
- **Риск:** средний — большие сгенерированные данные; не сломать тест целостности банка.
- **Промт для Codex:**
  > В проекте два банка слов: `data/words.py` (фразы → сид таблицы `words`) и `data/single_words_5000.py` (5000 одиночных слов → `LEARNING_WORDS`). Они пересекаются (напр. `apple`), но без единого источника истины — перевод можно поменять в одном и забыть в другом. Не ломая существующую структуру и тест целостности (`tests/test_openai_safety.py`, проверка что в `LEARNING_WORDS` нет пробелов), сведи переводы к одному источнику: либо сделай `single_words_5000` источником, из которого `words.py` берёт переводы для совпадающих слов, либо добавь проверку согласованности в тест. Опиши выбранный подход в комментарии. Прогони тесты.

### 5.2 🟡 Живые примеры употребления вместо шаблонных
- **Что сделать:** заменить шаблонные примеры `"Let's learn the word X."` на осмысленные (минимум для частотного ядра).
- **Файлы:** `data/single_words_5000.py`, `tools/relabel_words.py` (или новый генератор).
- **Ожидаемый результат:** карточки слов дают реальный контекст.
- **Риск:** средний — объём данных; качество примеров; для детей важна простота/безопасность.
- **Промт для Codex:**
  > В `data/single_words_5000.py` у большинства слов пример шаблонный: `"Let's learn the word X."`. Напиши скрипт-генератор (в `tools/`, по образцу `tools/relabel_words.py`) который заменяет шаблонные примеры на короткие осмысленные предложения уровня A1–A2, безопасные для детей, без оффлайн-вызова платных API по умолчанию (можно по шаблонам частотных конструкций или с опциональным флагом использования OpenAI). Не меняй формат кортежей и порядок записей. Сохрани идемпотентность (повторный запуск не дублирует). Прогони тесты.

### 5.3 🟡 Алгоритм интервального повторения (SRS)
- **Что сделать:** усилить выбор слов на повторение (статусы учим/повторить/выучено) лёгким SRS вместо чистого RANDOM.
- **Файлы:** `database.py` (`get_review_word`, `get_practice_word`, `update_progress`, `user_progress`), `webapp/server.py` (тренировки).
- **Ожидаемый результат:** слова всплывают на повторение по расписанию; лучше запоминание.
- **Риск:** средний — менять логику прогресса; миграция схемы (поле next_review_at).
- **Промт для Codex:**
  > Сейчас выбор слов для тренировки/повторения в `database.py` опирается на `ORDER BY RANDOM()` и статусы. Добавь лёгкий интервальный алгоритм (SM-2-lite): поле `next_review_at`/`interval`/`ease` в `user_progress` (миграция idempotent в `init_db`), обновление при `update_progress` (правильно → интервал растёт, ошибка → сброс), и выборку `get_review_word` по `next_review_at <= now`. Сохрани обратную совместимость API тренировок. Добавь юнит-тест логики интервалов. Прогони тесты и py_compile.

---

## 6. 🗣️ Улучшить голосовой модуль

### 6.1 🟠 Общий таймаут и устойчивость Realtime-инициализации
- **Что сделать:** ограничить суммарное время инициализации Realtime (сейчас retry даёт до ~50 с); понятная ошибка клиенту вместо зависания.
- **Файлы:** `webapp/openai_service.py:1136-1201`, `webapp/server.py:3589-3619`.
- **Ожидаемый результат:** быстрый отказ с сообщением, отсутствие «висящих» соединений.
- **Риск:** средний — не ухудшить успешные сценарии при кратком сбое OpenAI.
- **Промт для Codex:**
  > Инициализация Realtime-сессии (`_post_realtime_client_secret` в `webapp/openai_service.py` ~1136-1201) при ошибке делает retry и суммарно может держать соединение до ~50 секунд. Введи общий бюджет времени на весь обработчик `api_realtime_token` (например 30 секунд через `asyncio.wait_for`) и при исчерпании возвращай клиенту понятную ошибку (статус 504/502) вместо зависания. Сохрани один retry с минимальной конфигурацией, но в рамках общего бюджета. Прогони тесты и py_compile.

### 6.2 🟡 Устранить двойной вызов OpenAI при «русскоязычном ходе»
- **Что сделать:** оптимизировать `_needs_russian_repair` — избегать второго последовательного запроса там, где можно одним.
- **Файлы:** `webapp/openai_service.py:1481-1506`.
- **Ожидаемый результат:** −1 вызов OpenAI (latency и стоимость) на части голосовых ходов.
- **Риск:** средний — не потерять качество «починки» русскоязычного ответа.
- **Промт для Codex:**
  > В `webapp/openai_service.py` логика `_needs_russian_repair` (~1481-1506) при необходимости делает второй последовательный вызов OpenAI для «починки» ответа — это ×2 latency и стоимость. Проанализируй, можно ли получить корректный двуязычный ответ одним вызовом (через инструкции в промпте/формат ответа), оставив второй вызов только как редкий фолбэк. Реализуй с сохранением текущего качества и поведения. Покрой ключевую ветку юнит-тестом с моком. Прогони тесты и py_compile.

### 6.3 🟡 Единая нормализация возрастной группы
- **Что сделать:** свести три реализации нормализации `age_group` к одной общей функции (затрагивает и голос).
- **Файлы:** `webapp/lesson_engine.py:134`, `webapp/server.py:1569`, `webapp/openai_service.py:909`, новое место — `config.py` или `webapp/age.py`.
- **Ожидаемый результат:** единое поведение профилей возраста в чате, голосе и Realtime.
- **Риск:** низкий-средний — поведенческие отличия трёх версий нужно согласовать осознанно.
- **Промт для Codex:**
  > Нормализация `age_group` реализована трижды с расхождениями: `webapp/lesson_engine.py:134` (`normalize_age_group`), `webapp/server.py:1569` (`_normalized_age_group_for_user`, учитывает `child_age`), `webapp/openai_service.py:909` (`_normalize_realtime_age_group`). Сведи к одной функции (например в новом `webapp/age.py` или в `config.py`), принимающей возраст/группу и возвращающей канонический ключ из `AGE_GROUPS`. Замени три вызова на общую функцию, сохранив поведение «учёта `child_age`» там, где оно было. Прогони тесты (`test_lesson_engine`, `test_openai_safety`) и py_compile.

---

## 7. 🖼️ Улучшить генерацию картинок

### 7.1 🟠 Проверять наличие файла, а не только URL в БД
- **Что сделать:** при выдаче картинки слова проверять, что файл реально существует (после эфемерного сброса URL «протух»); иначе — корректный фолбэк/перегенерация по политике.
- **Файлы:** `webapp/server.py` (`_word_dict`, `_generated_vocab_url_exists`, выдача картинок), `database.py:632-651`.
- **Ожидаемый результат:** нет «битых» картинок после деплоя; перегенерация контролируемая.
- **Риск:** средний — связано с задачей 3.1; лучше делать после перехода на persistent storage.
- **Промт для Codex:**
  > После перехода кэша картинок на постоянное хранилище (см. отдельную задачу), убедись, что выдача картинки слова в `webapp/server.py` проверяет фактическое наличие объекта в хранилище, а не только непустой `generated_image_url` в БД (`database.py:632-651`). Если файла нет — отдавать фолбэк (эмодзи/SVG-сцена) и помечать статус для контролируемой перегенерации, без автоматического платного ре-гена на каждый запрос. Прогони тесты и py_compile.

### 7.2 🟡 Убрать лишний вызов build_vocabulary_visual
- **Что сделать:** `_problem_word_dict` дёргает полный `_word_dict` ради одной транскрипции — выделить лёгкую функцию транскрипции.
- **Файлы:** `webapp/server.py:984, 1075`, `webapp/vocabulary_visualizer.py`.
- **Ожидаемый результат:** меньше лишней работы на запросах со списком проблемных слов.
- **Риск:** низкий.
- **Промт для Codex:**
  > В `webapp/server.py` функция `_problem_word_dict` (~1075) вызывает весь `_word_dict` (~984), который запускает `build_vocabulary_visual`, только ради поля `transcription`. Выдели получение транскрипции в отдельную лёгкую функцию и используй её в `_problem_word_dict`, не запуская полную визуализацию. Поведение и формат ответа сохрани. Прогони тесты и py_compile.

### 7.3 🟡 Предгенерация/прогрев картинок частотного ядра
- **Что сделать:** офлайн-скрипт прогрева картинок для топ-N слов, чтобы не платить на горячем пути в рантайме.
- **Файлы:** `tools/` (новый скрипт), `webapp/openai_service.py`, хранилище из 3.1.
- **Ожидаемый результат:** мгновенная выдача картинок ядра; меньше стоимости в проде.
- **Риск:** средний — расходы на генерацию заранее; делать после 3.1.
- **Промт для Codex:**
  > Напиши офлайн-скрипт в `tools/` (по образцу существующих инструментов), который для топ-N самых частотных учебных слов заранее генерирует картинки через существующую функцию генерации и складывает в постоянное хранилище (см. задачу про storage), записывая URL/статус в БД. Скрипт идемпотентный (не регенерирует уже готовое), с лимитом и логированием стоимости. По умолчанию ничего не запускает без явного флага/числа N. Не трогай `.env`. Прогони py_compile.

---

## 8. ✅ Тестирование

### 8.1 🟠 Интеграционные тесты HTTP + auth + rate-limit
- **Что сделать:** добавить тесты на реальное aiohttp-приложение: проверка `auth_middleware` (валидный/невалидный initData), `hardening_middleware`, rate-limiter.
- **Файлы:** новый `tests/test_server_integration.py`, `webapp/server.py`, `webapp/auth.py`.
- **Ожидаемый результат:** регрессии в авторизации/лимитах ловятся автоматически.
- **Риск:** низкий-средний — нужны моки БД/OpenAI.
- **Промт для Codex:**
  > Добавь интеграционные тесты для aiohttp-приложения из `webapp/server.py`, используя `aiohttp.test_utils`. Покрой: (1) запрос к `/api/*` без `X-Telegram-Init-Data` → 401/403; (2) с валидной fallback-подписью (через `webapp/auth.make_fallback_auth_params`) → доступ; (3) заголовки безопасности из `hardening_middleware`; (4) срабатывание rate-limit при превышении лимита. БД и OpenAI замокай. Файл `tests/test_server_integration.py`. Прогони весь тест-набор.

### 8.2 🟠 Тесты слоя данных database.py
- **Что сделать:** покрыть критичные функции `database.py` (прогресс, очки, дневной урок, ai_usage) — на временной/мок-БД.
- **Файлы:** новый `tests/test_database.py`, `database.py`.
- **Ожидаемый результат:** рефакторинг БД перестаёт быть «слепым».
- **Риск:** средний — нужна тестовая БД (sqlite-несовместимость с asyncpg → либо локальный Postgres в CI, либо абстракция).
- **Промт для Codex:**
  > `database.py` (1531 строка) не покрыт тестами. Добавь `tests/test_database.py` для ключевой логики: `update_progress`, начисление очков, `daily_lesson` прогресс, `ai_usage` подсчёт за сутки, защита от дублей в finish-сценариях. Поскольку используется asyncpg/Postgres, выбери стратегию: либо тесты против локального Postgres (с пропуском, если `DATABASE_URL` тестовой БД не задан), либо вынеси чистую расчётную логику из SQL-функций и протестируй её изолированно. Опиши выбор в комментарии. Прогони тесты.

### 8.3 🟡 Тесты scoring и подсчёта результатов
- **Что сделать:** покрыть `api_vocab_finish`/`api_word_hunt_finish`/`api_level_submit` (очки, perfect-bonus, защита от повторной отправки).
- **Файлы:** `tests/`, `webapp/server.py`.
- **Ожидаемый результат:** гарантия корректности геймификации.
- **Риск:** низкий после появления service-слоя (см. блок 9).
- **Промт для Codex:**
  > Добавь тесты на подсчёт результатов: для `api_vocab_finish`, `api_word_hunt_finish`, `api_level_submit` (`webapp/server.py`) проверь корректность очков, perfect-bonus и защиту от повторной отправки одних и тех же ответов. Если логика плотно завязана на HTTP/БД — сначала выдели чистую расчётную функцию и протестируй её (согласуй с задачей рефакторинга service-слоя). Прогони тесты.

---

## 9. 🧹 Рефакторинг

> ⚠️ Делать **после** появления тестов (блок 8) — иначе рефакторинг вслепую. Каждую правку — отдельным маленьким PR.

### 9.1 🟡 Разбить server.py на модули
- **Что сделать:** выделить из `server.py` (3819 стр.): SVG-рендерер, rate-limiter, payload-билдеры, роутеры по доменам, inline-данные `LEVEL_TESTS`.
- **Файлы:** `webapp/server.py` → `webapp/svg_renderer.py`, `webapp/rate_limiter.py`, `webapp/payload_builders.py`, `webapp/routes/*.py`, `data/level_tests.py`.
- **Ожидаемый результат:** меньше merge-конфликтов, выше тестируемость.
- **Риск:** высокий — большой объём, легко задеть импорты/поведение. Только при зелёных тестах.
- **Промт для Codex:**
  > Постепенный рефакторинг `webapp/server.py` (3819 строк, God-object). Сделай ОДИН шаг за раз, прогоняя тесты после каждого: начни с выделения SVG-генератора (`_topic_icon_svg`, `_word_image_svg`, `_vocabulary_visual_svg`, строки ~452-883) в новый модуль `webapp/svg_renderer.py` без изменения поведения, обновив импорты. Не трогай остальную логику в этом шаге. Прогони тесты и py_compile. (Последующие шаги — rate_limiter, payload_builders, routes — отдельными задачами.)

### 9.2 🟡 Ввести service-слой
- **Что сделать:** вынести бизнес-цепочки из обработчиков (`api_chat_send`, `api_vocab_finish`, `api_daily_progress`) в `webapp/services/`.
- **Файлы:** `webapp/server.py` → `webapp/services/*.py`.
- **Ожидаемый результат:** логику можно тестировать без HTTP; тоньше обработчики.
- **Риск:** высокий — менять ядро логики; только с тестами.
- **Промт для Codex:**
  > Выдели бизнес-логику из «толстых» обработчиков `webapp/server.py` (`api_vocab_finish` ~2852, `api_chat_send` ~3251, `api_daily_progress` ~2613) в функции service-слоя (`webapp/services/`), оставив в обработчиках только парсинг запроса, вызов сервиса и формирование ответа. Поведение и контракт API — без изменений. Делай по одному обработчику за шаг, прогоняя тесты. Начни с `api_vocab_finish`.

### 9.3 🟡 Устранить дублирование выборки слова и подсчёта результатов
- **Что сделать:** вынести общий блок `_fetch_training_word` и общий подсчёт результатов finish-сценариев.
- **Файлы:** `webapp/server.py:3065-3114, 3153-3195, 2852, 2963`.
- **Ожидаемый результат:** одна копия логики вместо двух-трёх.
- **Риск:** средний.
- **Промт для Codex:**
  > В `webapp/server.py` дублируется логика: выборка тренировочного слова в `api_choice_next` (~3065) и `api_input_next` (~3153) — почти идентичный блок ~25 строк; подсчёт результатов в `api_vocab_finish` (~2852) и `api_word_hunt_finish` (~2963). Вынеси общую выборку в `_fetch_training_word(user_id, body, age_group)` и общий подсчёт результатов в отдельную функцию с параметром модели очков. Поведение сохрани, покрой тестом. Прогони тесты и py_compile.

---

## 📊 Сводка приоритетов

| Блок | Срочность | Главное |
|------|-----------|---------|
| 1. Срочно исправить | 🔴 сейчас | webhook-секрет, лимит Realtime, индексы, CSP |
| 3. Стабильность | 🔴 перед ростом | persistent storage, Redis |
| 4. Скорость | 🟠 | N+1, параллелизация, index.html |
| 8. Тестирование | 🟠 до рефакторинга | интеграционные + database.py |
| 5. Обучение словам | 🟡 | единый источник, SRS, живые примеры |
| 6. Голос | 🟡 | таймауты, двойной вызов, нормализация |
| 7. Картинки | 🟡 | проверка файла, прогрев |
| 2. Интерфейс | 🟡 | gzip, лоадеры, мобайл |
| 9. Рефакторинг | 🟡 после тестов | модули, service-слой, дубли |

*Связано: [[Технический аудит]] · [[00_Правила для Claude]]*
