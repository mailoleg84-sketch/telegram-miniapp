aiogram==3.13.1
asyncpg==0.30.0
aiohttp==3.10.10
openai>=1.99,<3.0
pronouncing==0.2.0
# Облачное хранилище кэшей (Cloudflare R2 / S3). Импортируется лениво — нужен
# только когда заданы R2_* (см. webapp/storage.py). Без R2 не используется.
aioboto3>=12,<14
